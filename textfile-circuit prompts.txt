//high end mouse 
"Create a high-level block diagram for a USB Optical Mouse circuit. The design should include six inputs (two for X/Y quadrature motion sensors and four for Left, Right, Middle, and DPI buttons). These inputs must feed into a central Multiplexer (MUX) that cycles through data channels, followed by a Microcontroller (MCU) block that formats the signals into an 8-bit USB HID packet for the final output."
//keyboard 
"Design a Keyboard Matrix Scanner circuit for a 104-key layout. The architecture must include an 8x13 Matrix Grid (8 Rows and 13 Columns), a 3-to-8 Line Decoder to drive the rows, and an 8-input Priority Encoder to read the columns. Show the logic for a Ghosting Prevention system using diodes and a central Microcontroller (MCU) that converts matrix coordinates into USB HID Scan Codes."
//ram
"Design a Synchronous DDR RAM module architecture. The circuit should feature a 10-bit Address Bus feeding into a Row/Column Decoder and an 8-bit Bidirectional Data Bus (DQ). Include a Differential Clock (CLK/CLK#) for double-data-rate timing and a Write Enable (WE) control line. The core should show a grid of D-Type Flip-Flops acting as memory cells with a Sense Amplifier at the output stage to stabilize the data signals."
//ddr ram 
"Create a high-level block diagram for a DDR4 SDRAM controller. The design should include a Command Decoder that receives commands from a Host Interface (CMD, ADDR, CAS#, RAS#, WE#), a Row/Column Address Latch that stores the incoming addresses, and a Bank Controller that manages the precharge, activate, read, and write operations. The core should show a 32-bit Data Bus (DQ) with a Differential Clock (CK/CK#) and a Write Enable (WE) control line. Include a Refresh Timer and a Self-Refresh Mode Controller to maintain data integrity."
//usb hub
"Design a USB 3.0 Hub Controller architecture. The circuit should feature a Host Interface (USB 3.0 SuperSpeed) feeding into a Command/Address Decoder, a Packet Router that directs data to the appropriate downstream ports, and a Port Controller for each downstream USB 3.0 port. Include a 10-bit Address Bus for port identification and a 32-bit Data Bus (SuperSpeed) for high-speed data transfer. The core should show a Grid of Port Controllers with a Central Arbiter to manage bandwidth allocation."
//advanced drone
"Design an Advanced Autonomous Drone Flight Stack using a multi-core SoC architecture. The system must integrate a Sensor Fusion Hub as the primary input stage, merging data from a 6-axis IMU, LiDAR rangefinder, and GPS. The Next State Logic must implement a Kalman Filter for precise positioning and a Pathfinding Algorithm for real-time obstacle avoidance. The Output Logic should generate four independent, high-resolution DShot digital signals for the Electronic Speed Controllers (ESCs) and a dedicated Telemetry link for ground station communication."
//usb pen drive 
"Design a USB 2.0 Flash Drive Controller architecture. The circuit should feature a USB 2.0 Interface (Full-Speed) feeding into a Command Decoder, a Flash Memory Controller that manages NAND Flash erase, program, and read operations, and a Built-in Flash Translation Layer (FTL) that maps logical addresses to physical blocks. Include a 32-bit Data Bus (DQ) and a 10-bit Address Bus for memory addressing. The core should show a Grid of NAND Flash Memory Cells with a Wear-Leveling Algorithm and a Bad-Block Management System."
//ai chip npu
Design a Sustolic Array NPU architecture for edge-AI processing. The circuit should feature a 4x4 grid of MAC (Multiply-Accumulate) Units, a dedicated Weight Memory Buffer, and an Activation Function Block (ReLU/Sigmoid) at the output stage. The data path should be optimized for INT8 quantization to minimize power consumption.
//nano chip 
"Design an Ultra-Low-Power 4-bit ALU for a biomedical nano-sensor. The circuit must feature independent ADDER, SUBTRACTOR, and Bitwise AND/OR units feeding into a 4-to-1 Multiplexer. Include a Sleep-Enable input to gate the power to unused logic blocks and a Feedback Loop to store the result in a 4-bit Accumulator register for high-efficiency data logging."
//hdl circuit of 10000 input and ouputs compressed form 
"Design a High-Performance Many-Core Processor (MPSoC) architecture. The circuit should feature a tile-based mesh network (NoC) consisting of 64 independent processing cores. Each core must include a 32-bit RISC-V ALU, a private L1 Cache (SRAM), and a Memory Management Unit (MMU). Integrate a Global Crossbar Switch to manage thousands of interconnects between the cores and a high-bandwidth DDR5 Memory Controller interface. Specify the Clock Tree Synthesis (CTS) strategy to minimize skew across the entire 10,000+ gate fabric."
//m1 chip 
Design a Heterogeneous ARMv8 SoC similar to the Apple M1. The circuit should feature a High-Performance ALU cluster (Adder/Subtractor/Logic) and a Power-Efficient ALU cluster sharing a 128-bit wide Unified Data Bus. Implement an Out-of-Order Execution Dispatcher using a MUX-based selector to route results to the final output registers. Include a Shared L2 Cache block to minimize latency between the memory inputs and the processing units."